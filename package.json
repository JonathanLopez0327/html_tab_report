{
  "name": "html_tab_report",
  "version": "1.9.0",
  "description": "Extensión de Azure DevOps para visualizar reportes K6",
  "scripts": {
    "build": "npm run copy-files && npm run build-task",
    "copy-files": "if not exist dist mkdir dist && copy report-tab.html dist\\report-tab.html",
    "build-task": "pwsh -File build-task.ps1",
    "package": "npm run build && tfx extension create --manifest-globs vss-extension.json",
    "publish": "tfx extension publish --manifest-globs vss-extension.json"
  },
  "keywords": [
    "azure-devops",
    "extension",
    "k6",
    "report"
  ],
  "author": "JonathanLopez",
  "license": "ISC",
  "dependencies": {
    "vss-web-extension-sdk": "^5.141.0"
  },
  "devDependencies": {
    "tfx-cli": "^0.22.1"
  }
}
